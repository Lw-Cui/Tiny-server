INCLUDE_DIRECTORIES(${SERVER_INCLUDE_DIRS})

ADD_EXECUTABLE(peerA peerA.cpp)
ADD_EXECUTABLE(peerB peerB.cpp)
TARGET_LINK_LIBRARIES(peerA ${SERVER_LIBRARIES})
TARGET_LINK_LIBRARIES(peerB ${SERVER_LIBRARIES})
