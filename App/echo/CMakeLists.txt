INCLUDE_DIRECTORIES(${SERVER_INCLUDE_DIRS})

ADD_EXECUTABLE(echoServer Server.cpp)
ADD_EXECUTABLE(echoClient client.cpp)
TARGET_LINK_LIBRARIES(echoServer ${SERVER_LIBRARIES} pthread)
TARGET_LINK_LIBRARIES(echoClient ${SERVER_LIBRARIES})